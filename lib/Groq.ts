// lib/Groq.ts
import OpenAI from "openai";

const groq = new OpenAI({
  apiKey: process.env.GROQ_API_KEY,
  baseURL: "https://api.groq.com/openai/v1",
});

export async function generatePlaylist(prompt: string, songs: any[]) {
  console.log("🎵 Songs sent to AI:", songs);
  const fullPrompt = `
คุณคือนักจัด Playlist อัจฉริยะ 

คำขอของผู้ใช้: "${prompt}"

ข้อมูลเพลง:
${JSON.stringify(songs, null, 2)}

โปรดเลือกเพลงจากข้อมูลด้านบนที่เหมาะสมที่สุดกับคำขอของผู้ใช้ ค้นหาเพลงนีเป็นอย่างไรก่อนเลือก

โปรดตอบกลับเฉพาะ JSON ในรูปแบบนี้อย่างเดียว:
{
  "playlist": [/* รหัสเพลงที่เลือก (string array) */],
  "reason": "คำอธิบายเหตุผลการเลือกเพลงอย่างละเอียด เเต่ไม่ต้องบอกว่าเลือกมาจาก tag"
}

ห้ามตอบข้อความอื่น ๆ นอกจาก JSON นี้
`;

  const res = await groq.chat.completions.create({
    model: "meta-llama/llama-4-scout-17b-16e-instruct",
    messages: [
      {
        role: "system",
        content:
          "คุณคือผู้ช่วยแนะนำเพลงที่เข้าใจความรู้สึกจากเพลง ไม่ใช้แค่ tag",
      },
      { role: "user", content: fullPrompt },
    ],
    temperature: 0.5,
  });

  const reply = res.choices[0].message.content;
  console.log("AI raw reply:", reply);

  try {
    // พยายามหา JSON object ต้นแรกในข้อความตอบกลับ
    const jsonStart = reply.indexOf("{");
    if (jsonStart === -1) {
      throw new Error("No JSON object found in AI reply");
    }
    const jsonText = reply.slice(jsonStart);
    return JSON.parse(jsonText);
  } catch (e) {
    console.error("Failed to parse AI reply as JSON:", e);
    return { playlist: [], reason: "" };
  }
}

// export async function streamGeneratePlaylist(prompt: string, songs: any[]) {
//   const systemPrompt = `
// คุณคือผู้ช่วยที่ทำหน้าที่เลือกเพลงจากฐานข้อมูลด้านล่าง
// - เลือกเพลงที่ตรงกับอารมณ์/ข้อความที่ผู้ใช้ให้มา
// - อธิบายเหตุผลอย่างเป็นธรรมชาติว่าทำไมถึงเลือกเพลงแต่ละเพลง
// - ตอบกลับเป็นข้อความภาษามนุษย์ ไม่ใช่ JSON
// `;

//   const userPrompt = `
// ข้อความจากผู้ใช้: ${prompt}

// ฐานข้อมูลเพลงที่คุณมี:
// ${songs.map(
//     (s) =>
//       `- ${s.name_song} (${s.song_tags}) : ${s.description ?? "ไม่มีคำอธิบาย"}`
//   ).join("\n")}

// กรุณาเลือกเพลงที่เหมาะสม และอธิบายเหตุผลในการเลือก
// `;

//   return groq.chat.completions.create({
//     model: "mixtral-8x7b-32768", // หรือรุ่นอื่นที่คุณใช้
//     stream: true,
//     messages: [
//       { role: "system", content: systemPrompt },
//       { role: "user", content: userPrompt },
//     ],
//   });
// }
